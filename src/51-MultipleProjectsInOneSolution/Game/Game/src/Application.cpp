#include "Engine.h"
#include<iostream>

/*
* The project Game will reference the project Engine in the same solution by using static linking
*/

int main() 
{
	engine::PrintMessage();
	std::cin.get();

}